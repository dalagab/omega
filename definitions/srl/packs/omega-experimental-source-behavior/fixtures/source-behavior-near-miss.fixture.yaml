schema: omega.sigmascope.rule-fixture.v1
name: source behavior unrelated observations do not imply market purchasing
observations:
  sourceOperations:
    - operationId: op-request
      operation: network.http.request
      serviceCapabilities: [source.repository]
  sourceDataFlow:
    - edgeId: data-request-log
      relation: value-used-by
      fromOperation: network.http.request
      toOperation: data.select
      fromServiceCapabilities: [source.repository]
  sourceFlowEdges:
    - edgeId: flow-move-interact
      relation: happens-before
      fromOperation: game.character.move
      toOperation: game.marketboard.interact
      minimumDelayMs: 0
expected:
  facts: []
  matchedRules: []
  findingIds: []
