name: Omega catalog discovery launcher

on:
  schedule:
    - cron: "41 */6 * * *"
  workflow_dispatch:

permissions:
  contents: write
  issues: read

jobs:
  call-discovery-worker:
    uses: dalagab/omega/.github/workflows/catalog-discovery.yml@sigmascope
    secrets: inherit
