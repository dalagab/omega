#!/usr/bin/env python3
# Retired in Omega 0.8.0.0; SQLite validation is built into the builder/workflow.
raise SystemExit("Retired in Omega 0.8.0.0; SQLite validation is built into the builder/workflow.")
