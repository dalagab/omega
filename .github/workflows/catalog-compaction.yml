name: Omega security services · legacy compactor launcher

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  call-security-branch:
    uses: dalagab/omega/.github/workflows/catalog-compaction.yml@sigmascope
    secrets: inherit
