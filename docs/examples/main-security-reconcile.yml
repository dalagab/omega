name: Omega security services · reconciliation launcher

on:
  schedule:
    - cron: "17 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  actions: write

jobs:
  reconcile-security-work:
    uses: dalagab/omega/.github/workflows/security-reconcile.yml@sigmascope
    secrets: inherit
