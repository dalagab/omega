{
  "name": "omega-github-pages",
  "private": true,
  "scripts": {
    "build:pages": "python tools/site/build_site.py && npx @tailwindcss/cli -i ./site/assets/tailwind.css -o ./_site/assets/site.css --minify && python tools/site/validate_site.py"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
