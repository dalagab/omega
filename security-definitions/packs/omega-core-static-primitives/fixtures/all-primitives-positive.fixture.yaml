schema: omega.sigmascope.rule-fixture.v1
name: all migrated primitive facts from retained literal observations
observations:
  staticPatternMatches:
    - {origin: artifact, pattern: HttpWebRequest, evidenceLabel: "metadata:Fixture.dll", evidence: ["metadata:Fixture.dll: HttpWebRequest"]}
    - {origin: artifact, pattern: TcpClient, evidenceLabel: "metadata:Fixture.dll", evidence: ["metadata:Fixture.dll: TcpClient"]}
    - {origin: artifact, pattern: Process.Start, evidenceLabel: "metadata:Fixture.dll", evidence: ["metadata:Fixture.dll: Process.Start"]}
    - {origin: source, pattern: pwsh.exe, evidenceLabel: "source:Fixture.cs", evidence: ["source:Fixture.cs: pwsh.exe"]}
    - {origin: artifact, pattern: CredRead, evidenceLabel: "native-pe:Fixture.dll", evidence: ["native-pe:Fixture.dll: CredRead"]}
expected:
  facts:
    - credential.api
    - network.http
    - network.socket
    - process.launch
    - shell.powershell
  matchedRules:
    - primitive.credential.api
    - primitive.network.http
    - primitive.network.socket
    - primitive.process.launch
    - primitive.shell.powershell
  findingIds: []
