name: Omega Deep Scan queue worker

on:
  workflow_dispatch:
  schedule:
    - cron: "37 * * * *"

permissions:
  contents: write

jobs:
  deep-scan:
    uses: dalagab/omega/.github/workflows/deep-scan.yml@sigmascope
