{
  "schema": "omega.sigmascope.yara-policy.v1",
  "semantics": "supplemental-evidence-only",
  "defaultRuleState": "disabled-unless-reviewed",
  "allowedStatuses": [
    "enabled",
    "disabled"
  ],
  "requiredMetadata": [
    "schema",
    "ruleFile",
    "ruleNames",
    "status",
    "provenance",
    "license",
    "reviewedAtUtc",
    "falsePositiveExpectation",
    "scope",
    "reviewNotes"
  ],
  "policy": [
    "A YARA match is secondary evidence and never a standalone SigmaScope verdict.",
    "Every rule must have a same-name .metadata.json sidecar and must default to disabled until reviewed.",
    "Rule provenance and licensing must be explicit and reviewable.",
    "Rules with broad or heuristic conditions must document their expected false-positive behavior.",
    "Rule and metadata changes are frozen into Definitions and invalidate artifact-analysis cache identity."
  ]
}
